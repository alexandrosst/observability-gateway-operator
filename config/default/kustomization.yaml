resources:
- ../rbac
- ../manager

namespace: observability-system

images:
- name: controller
  newName: alexandrosst/observability-gateway-operator
  newTag: v0.1.0  # Change to whatever tag you pushed (or 'latest' for testing)

labels:
- pairs:
    app.kubernetes.io/name: observability-gateway-operator
    app.kubernetes.io/instance: observability-gateway-operator
    app.kubernetes.io/part-of: observability-gateway-operator
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/created-by: controller-manager
  includeSelectors: true
  includeTemplates: true